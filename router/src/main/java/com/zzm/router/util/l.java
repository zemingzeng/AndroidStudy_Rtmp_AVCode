package com.zzm.router.util;

import android.util.Log;

public class l {
    private static final String TAG = "22m";
    private static final boolean DEBUG = true;

    public static void i(String info) {
        if (DEBUG)
            Log.i(TAG, info);
    }

    public static void i(String TAG, String info) {
        if (DEBUG)
            Log.i(TAG, info);
    }
}
